const end = () => {

    return (
        <div>
            <section className="home">
                <h1>The End guys</h1>

                <br />

                <button onClick={() => window.location.href = '/'}>HOME</button>

            </section>
        </div>
    );
}

export default end;